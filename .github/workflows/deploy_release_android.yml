name: Deploy experimental android release
on:
  workflow_dispatch:
    inputs:
      version_major:
        description: Version major
        default: "2"
      version_minor:
        description: Version minor
        default: "0"

jobs:
  deploy:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          submodules: "recursive"
          fetch-depth: 0

      - name: set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Install Dependencies
        run: |
          sudo apt update -qq
          sudo apt install -y gettext mono-complete ninja-build cmake

      - name: Build Cemu
        run: |
          # 1. Encontrar la carpeta de Android automáticamente
          DIR=$(find . -name "gradlew" -printf '%h\n' | head -n 1)
          cd "$DIR"
          
          # 2. Limpieza profunda de caché de C++ y Gradle
          chmod +x gradlew
          ./gradlew clean

          # 3. Compilar ignorando advertencias menores que detienen el proceso
          ./gradlew assembleRelease --stacktrace --info

      - name: Upload APK
        if: success()
        run: |
          VERSION="${{ inputs.version_major }}.${{ inputs.version_minor }}"
          mkdir -p upload
          find . -name "*.apk" -exec mv {} upload/Cemu-$VERSION.apk \;
          
          wget -O ghr.tar.gz https://github.com/tcnksm/ghr/releases/download/v0.15.0/ghr_v0.15.0_linux_amd64.tar.gz
          tar xvzf ghr.tar.gz; rm ghr.tar.gz
          ./ghr -u ${{ github.repository_owner }} -r ${{ github.event.repository.name }} -token ${{ secrets.GITHUB_TOKEN }} v$VERSION upload/
          
